# ==========================================
# 本地开发环境配置模板
# ==========================================
# 使用场景: 在本机开发，前后端都在 localhost
# 使用方法: 默认配置，无需额外操作
# 访问地址: http://localhost:5173

# Vite 服务器配置
VITE_HOST=127.0.0.1
VITE_PORT=5173

# 后端 API 地址（通过 Vite 代理访问本地后端）
VITE_API_BASE_URL=/api

# Vite 代理目标（后端地址）
VITE_PROXY_TARGET=http://localhost:8000